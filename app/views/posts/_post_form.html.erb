<!--Este archivo se titula _post_form con el fin de renderizar cosas.-->
<%= form_for @post do |p| %>
  <%= p.text_field :title %>
  <%= p.text_field :description %>
  <%= p.submit locals[:b_text] %>
<% end %>
